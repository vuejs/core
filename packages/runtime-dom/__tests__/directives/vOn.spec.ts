describe('runtime-dom: v-on directive', () => {
  test('it should support modifier guards', () => {})

  test('it should support keys like .esc', () => {})

  test('it should support "exact" modifier', () => {})
})
